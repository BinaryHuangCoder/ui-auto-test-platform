# UI 自动化测试平台 - 详细设计文档

## 1. 系统架构设计

### 1.1 整体架构
系统采用前后端分离架构，分为四层：
- **表现层**：Vue 3 前端应用，提供用户交互界面
- **业务层**：Spring Boot 后端服务，处理业务逻辑
- **数据层**：MySQL 数据库，存储测试用例和执行记录
- **执行层**：Node.js + Playwright + Midscene，执行测试用例

### 1.2 模块划分
```
ui-auto-test-platform/
├── frontend/          # 前端模块
│   ├── src/
│   │   ├── views/     # 页面组件
│   │   ├── components/# 通用组件
│   │   ├── api/       # API 调用
│   │   └── utils/     # 工具函数
│   └── dist/          # 构建输出
├── backend/           # 后端模块
│   ├── src/main/java/com/uiauto/
│   │   ├── controller/# 控制器层
│   │   ├── service/   # 服务层
│   │   ├── entity/    # 实体类
│   │   ├── mapper/    # 数据访问层
│   │   └── model/     # 数据模型
│   └── target/        # 编译输出
└── scripts/           # 执行脚本
    ├── executor.js    # 测试执行器
    └── executor-local.sh # 本地执行脚本
```

## 2. 数据库设计

### 2.0 测试任务表 (test_task)
```sql
CREATE TABLE test_task (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    task_no VARCHAR(100) NOT NULL UNIQUE COMMENT '任务编号',
    task_name VARCHAR(200) NOT NULL COMMENT '任务名称',
    cron_expression VARCHAR(100) COMMENT 'Cron表达式',
    concurrency INT DEFAULT 1 COMMENT '并发数（1-10）',
    task_config TEXT COMMENT '任务配置(JSON格式)',
    creator VARCHAR(50) COMMENT '创建人',
    status TINYINT DEFAULT 1 COMMENT '状态：1-启用 0-禁用',
    remark TEXT COMMENT '备注',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间'
) COMMENT='测试任务表';
```

### 2.1 测试用例表 (test_case)
```sql
CREATE TABLE test_case (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(200) NOT NULL COMMENT '用例名称',
    description TEXT COMMENT '用例描述',
    category VARCHAR(50) COMMENT '分类',
    status TINYINT DEFAULT 1 COMMENT '状态：1-启用 0-禁用',
    created_by VARCHAR(50) COMMENT '创建人',
    created_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 2.2 测试步骤表 (test_case_step)
```sql
CREATE TABLE test_case_step (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    case_id BIGINT NOT NULL COMMENT '用例 ID',
    step_no INT NOT NULL COMMENT '步骤序号',
    step_type VARCHAR(20) COMMENT '步骤类型：open/click/type/wait/ai-query',
    step_description TEXT COMMENT '步骤描述',
    target VARCHAR(500) COMMENT '目标元素/URL',
    value VARCHAR(500) COMMENT '输入值',
    assertion_description TEXT COMMENT '断言描述',
    screenshot TINYINT DEFAULT 0 COMMENT '是否截图：1-是 0-否',
    created_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 2.3 用例执行记录表 (test_case_execution)
```sql
CREATE TABLE test_case_execution (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    case_id BIGINT NOT NULL COMMENT '用例 ID',
    case_no VARCHAR(100) COMMENT '用例编号',
    case_name VARCHAR(200) COMMENT '用例名称',
    description TEXT COMMENT '执行描述',
    executor VARCHAR(50) COMMENT '执行人',
    start_time DATETIME COMMENT '开始时间',
    end_time DATETIME COMMENT '结束时间',
    duration BIGINT COMMENT '执行时长 (ms)',
    total_count INT COMMENT '总步骤数',
    passed_count INT COMMENT '通过步骤数',
    failed_count INT COMMENT '失败步骤数',
    ai_total_token_used BIGINT DEFAULT 0 COMMENT 'AI token总消耗',
    error_message TEXT COMMENT '错误信息',
    status VARCHAR(20) COMMENT '状态：running/success/failed',
    created_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 2.4 步骤执行记录表 (test_step_execution)
```sql
CREATE TABLE test_step_execution (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    execution_id BIGINT NOT NULL COMMENT '执行记录 ID',
    step_no INT NOT NULL COMMENT '步骤序号',
    step_description TEXT COMMENT '步骤描述',
    duration BIGINT COMMENT '执行时长 (ms)',
    status VARCHAR(20) COMMENT '状态：success/failed',
    screenshot TEXT COMMENT '截图 Base64',
    assertion_description TEXT COMMENT '断言描述',
    assertion_status VARCHAR(20) COMMENT '断言状态',
    ai_result TEXT COMMENT 'AI 执行结果',
    ai_token_used BIGINT DEFAULT 0 COMMENT 'AI token消耗',
    error_message TEXT COMMENT '错误信息',
    created_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

## 3. 接口设计

### 3.1 测试用例管理
| 接口 | 方法 | 路径 | 描述 |
|------|------|------|------|
| 查询用例列表 | GET | /api/case/list | 分页查询用例 |
| 获取用例详情 | GET | /api/case/{id} | 获取单个用例 |
| 创建用例 | POST | /api/case | 创建新用例 |
| 更新用例 | PUT | /api/case/{id} | 更新用例信息 |
| 删除用例 | DELETE | /api/case/{id} | 删除用例 |

### 3.2 测试执行
| 接口 | 方法 | 路径 | 描述 |
|------|------|------|------|
| 执行用例 | POST | /api/execution/run/{caseId} | 执行单个用例 |
| 批量执行 | POST | /api/execution/runBatch | 批量执行用例 |
| 查询执行列表 | GET | /api/execution/list | 分页查询执行记录 |
| 获取执行详情 | GET | /api/execution/{id} | 获取执行详情 |
| 获取步骤详情 | GET | /api/execution/steps/{executionId} | 获取步骤执行详情 |

## 4. 核心流程设计

### 4.1 测试执行流程
```
1. 用户触发执行 → 2. 创建执行记录 → 3. 加载测试步骤
       ↓
4. 初始化浏览器 → 5. 循环执行步骤 → 6. 捕获截图
       ↓
7. 执行 AI 断言 → 8. 保存执行结果 → 9. 关闭浏览器
       ↓
10. 更新执行状态 → 11. 返回执行结果
```

### 4.2 截图处理流程
```
1. 步骤执行完成 → 2. Playwright 截图 → 3. 转 Base64 编码
       ↓
4. 返回 JSON 结果 → 5. 后端提取 Base64 → 6. 保存到数据库
       ↓
7. 前端展示截图
```

## 5. 关键代码设计

### 5.1 执行器核心逻辑 (executor.js)
```javascript
async function executeStep(action, target, value) {
  const result = { success: false, screenshot: null, screenshotBase64: null };
  
  // 执行动作
  switch (action) {
    case 'open': await page.goto(target); break;
    case 'click': await page.click(target); break;
    case 'type': await page.fill(target, value); break;
    case 'ai-query': const queryResult = await agent.ai(target); break;
  }
  
  // 截图
  const screenshot = await page.screenshot();
  result.screenshotBase64 = 'data:image/png;base64,' + screenshot.toString('base64');
  
  return result;
}
```

### 5.2 后端截图提取逻辑
```java
private String extractScreenshot(String result) {
    // 从 screenshotBase64 字段获取（长度 20 字符）
    int base64Start = result.indexOf("\"screenshotBase64\":\"");
    if (base64Start >= 0) {
        base64Start += 20;  // 跳过字段名
        int base64End = result.indexOf("\"", base64Start);
        return result.substring(base64Start, base64End);
    }
    return null;
}
```

## 6. 变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.0 | 2026-04-03 | 初始设计 | huangzhiyong081439 |
| v1.1 | 2026-04-05 | 增加 AI 断言设计 | huangzhiyong081439 |
| v1.2 | 2026-04-13 | 新增测试任务管理、AI token消耗统计功能设计 | huangzhiyong081439 |
| v1.3 | 2026-04-15 | 新增模型管理、场景模型配置、双重缓存机制、实时进度更新、节约模式、详细耗时统计、token消耗明细等功能设计 | huangzhiyong081439 |
| v1.4 | 2026-04-16 | 新增AI断言耗时列、token消耗明细列、重新排序步骤执行记录表格、优化总耗时计算逻辑 | huangzhiyong081439 |
| v1.5 | 2026-04-16 | 新增清空测试用例缓存功能、执行进度条显示、列显示/隐藏功能（所有列表页面） | huangzhiyong081439 |
| v1.6 | 2026-04-16 | 模型管理新增模型描述字段、修复测试连接功能（支持火山方舟/阿里百炼等OpenAI兼容协议）、测试连接按钮添加防重提功能 | huangzhiyong081439 |
| v1.7 | 2026-04-17 | 新增测试任务并发执行功能设计：test_task表新增concurrency字段、后端使用线程池实现并发执行、前端支持滑块+输入框配置并发数 | 小龙虾 |
